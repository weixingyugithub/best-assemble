<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!--表明重用预编译的SQL语句 -->
	<settings>
		<setting name="defaultExecutorType" value="REUSE" />
<!-- 		<setting name="logPrefix" value="dao." /> -->
	</settings>
	
	<!--类型别名定义。今后可只用Student来代替它冗长的全限定名 -->
<!-- <typeAliases>
		<typeAlias alias="Person"
			type="com.example.mybatis.personmg.bo.EcExamplePersonmgBOExt" />
	</typeAliases> -->
	
	<!--environments可包含多个environment元素。每个environment配置与数据库交互的细节，这里只需要配置一个。default属性是指在创建SqlSessionFactory时，若没有明确指定要用哪个environment，则使用此属性指定的 -->
<!-- 	<environments default="development">
		<environment id="development">
			<transactionManager type="jdbc" />
			<dataSource type="POOLED">
				<property name="driver" value="oracle.jdbc.driver.OracleDriver" />
				<property name="url" value="jdbc:oracle:thin:@10.200.8.45:1521:swmdb" />
				<property name="username" value="swm" />
				<property name="password" value="swmdba" />
			</dataSource>
		</environment>
	</environments> -->
	
	<!--指定要用到的mapper文件。以下的resource属性告诉MyBatis要在类路径下的resources目录下找StudentMapper.xml文件。我们将把mapper文件存放在src目录下的resources目录中，并让ant把resources目录拷贝到classes中，而classes目录会被ant添加到类路径中。 -->
<!-- 	<mappers>
		<mapper resource="/META-INF/orm/oracle/Student-mapper.xml" />
	</mappers> -->

<!-- 	<plugins>
		<plugin interceptor="com.github.pagehelper.PageHelper">
			<property name="dialect" value="oracle" />
			<property name="offsetAsPageNum" value="true" />
			<property name="rowBoundsWithCount" value="true" />
			<property name="pageSizeZero" value="true" />
			<property name="reasonable" value="false" />
			<property name="params"
				value="pageNum=pageHelperStart;pageSize=pageHelperRows;" />
			<property name="supportMethodsArguments" value="false" />
			<property name="returnPageInfo" value="none" />
		</plugin>
	</plugins> -->


</configuration>